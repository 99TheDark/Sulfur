for i := 12; i <= 20; i++ {
    age := string!(i)
    if i >= 18 {
        println("It is legal to drink at " + age + " years old")
    } else {
        println("It is illegal to drink at " + age + " years old")
    }
}

println(". . . . . . . . . . . . . ")

/*
entry    -> for.cond
for.cond -> for.body, for.end
for.body -> if.body, if.else
if.body  -> if.end
if.else  -> if.end
if.end   -> for.inc
for.inc  -> for.cond
for.end  -> exit
exit
*/